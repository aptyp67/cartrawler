# CarTrawler — Car Availability Viewer

Displays live car rental offers from CarTrawler’s JSON feed. Browse, sort by price, and view details.

- Start dev: `yarn install && yarn dev`
- Build: `yarn build`  •  Preview: `yarn preview`
